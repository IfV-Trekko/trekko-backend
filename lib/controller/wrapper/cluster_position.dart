import 'package:fluster/fluster.dart';

class ClusterPosition extends Clusterable {
  ClusterPosition({latitude, longitude, clusterId})
      : super(latitude: latitude, longitude: longitude, clusterId: clusterId);
}
